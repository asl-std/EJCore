name: Build

on:
  push:
    branches: [ "snapshot" ]
  pull_request:
    branches: [ "snapshot" ]

jobs:
  Package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Build with Maven
        run: mvn install --file pom.xml